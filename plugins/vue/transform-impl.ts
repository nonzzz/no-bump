import type { TransformParams } from './interface'

export const transformScriptImpl = ({ input, options, pluginContext }: TransformParams) => {
  //
}

export const transformTemplateImpl = ({ input, options, pluginContext }: TransformParams) => {
  //
}

export const transformStyleImpl = ({ input, options, pluginContext }: TransformParams) => {
  //
}
