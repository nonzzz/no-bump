import test from 'ava'
import { Vue } from '..'

test('transform', async (t) => {
  //   const plugin = Vue()
  //   const { code } = await plugin.transform('<template></template>', './src/index.vue')
  //   t.is(code, '<template></template>')
})
